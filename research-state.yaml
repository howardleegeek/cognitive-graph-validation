current_round: 194
last_action: "H1.428 - Hybrid Architecture. Tested combining Per-Object CG (for perception) with 2-Node CG (for action prediction). Found that Hybrid (avg MSE 1.139827) does NOT outperform best individual Per-Object CG (1.113304). Baseline still wins on synthetic data. Hypothesis NOT_SUPPORTED."
next_intended_action: "H1.429 - Test Per-Object CG with temporal sequence modeling (LSTM/GRU) to handle multi-step tasks better, addressing the weakness found in H1.425."

experiments:
  total_rounds: 194
  trajectory:
    - experiment_id: H1.428
      description: "Hybrid Architecture - Combining Per-Object CG with 2-Node CG"
      result:
        conclusion: "NOT_SUPPORTED"
        task: "hybrid_architecture"
        results:
          baseline_avg_mse: 1.085781
          per_object_avg_mse: 1.113304
          two_node_avg_mse: 1.132485
          hybrid_avg_mse: 1.139827
          spatial_relations:
            baseline: 1.111649
            per_object: 1.138354
            two_node: 1.139350
            hybrid: 1.133744
          multi_stage:
            baseline: 1.059912
            per_object: 1.088253
            two_node: 1.125620
            hybrid: 1.145911
        key_insight: "Hybrid architecture does not outperform individual architectures. Per-Object CG remains the best CG variant. Additional complexity adds overhead without benefit on synthetic data."
        config:
          n_demos: 500
          n_runs: 3
          epochs: 30
          architectures: ["Baseline", "Per-Object CG", "2-Node CG", "Hybrid"]
          task_types: ["spatial_relations", "multi_stage"]
    - experiment_id: H1.427
      description: "Task Type Transfer Learning - Testing if Per-Object CG overfits to task-specific features"
      result:
        conclusion: "REFUTED"
        task: "transfer_learning"
        results:
          baseline_avg_transfer_gap: 4305.08
          two_node_avg_transfer_gap: 1757.24
          per_object_avg_transfer_gap: 1076.91
          spatial_to_multi_gaps:
            baseline: 105.38
            two_node: 90.34
            per_object: 75.66
          multi_to_spatial_gaps:
            baseline: 8504.77
            two_node: 3424.14
            per_object: 2078.17
        key_insight: "Per-Object CG transfers BEST across task types, contradicting the hypothesis that it overfits. The earlier finding that Per-Object CG performs worse on multi-stage tasks (H1.425) is due to task structure, not overfitting. Object-centric features are generalizable but less useful for sequential manipulation."
        config:
          n_demos: 500
          n_runs: 3
          epochs: 30
          finetune_epochs: 10
          architectures: ["Baseline", "2-Node CG", "Per-Object CG"]
          task_types: ["spatial_relations", "multi_stage"]
