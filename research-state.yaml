current_round: 276
last_action: "H1.470.1.1.37 - Test adaptive regularization that scales with model capacity (REFUTED - fixed regularization consistently outperforms all adaptive strategies across model sizes 32-128)"
next_intended_action: "H1.470.1.1.38 - Test whether over-regularization at h=256 is architecture-dependent (multi-layer GRU, layer norm) vs purely capacity-dependent; or investigate learned regularization weights via meta-learning"

experiments:
  total_rounds: 276
  trajectory:
    - experiment_id: H1.470.1.1.37
      description: "Test adaptive regularization that scales with model capacity"
      result:
        conclusion: "REFUTED"
        task: "multi_step_manipulation"
        configurations_tested: 30
        model_sizes_tested: [32, 64, 128]
        data_volumes_tested: [500, 2000]
        strategies_tested: ["baseline", "fixed", "adaptive_linear", "adaptive_inverse_sqrt", "adaptive_exponential"]
        key_metrics:
          h32_best: "fixed (+0.04%)"
          h64_best: "fixed (+0.10%)"
          h128_best: "fixed (+0.11%)"
          fixed_h32: "+0.04%"
          fixed_h64: "+0.10%"
          fixed_h128: "+0.11%"
          adaptive_linear_h128: "+0.04%"
          adaptive_exponential_h128: "+0.02%"
        key_insights:
          - "Fixed regularization (weight=0.1) consistently outperforms all adaptive strategies"
          - "Simple scaling functions (linear, inverse sqrt, exponential) are too aggressive in reducing weight for larger models"
          - "Over-regularization effect from H1.470.1.1.36 may be architecture-dependent, not purely capacity-dependent"
          - "Effect sizes are small (0.02-0.11%) suggesting limited impact of temporal consistency regardless of scaling"
        recommendations:
          - "R1: Fixed regularization weight (0.1) is robust across model sizes 32-128"
          - "R2: Investigate whether over-regularization at h=256 is architecture-dependent"
          - "R3: Consider learned regularization weights (meta-learning) instead of hand-designed scaling"
          - "R4: Test with full architecture to reproduce H1.470.1.1.36 conditions"
    
    - experiment_id: H1.470.1.1.36
      description: "Test whether auxiliary loss benefits scale with model size and data volume"
      result:
        conclusion: "REFUTED"
        task: "multi_step_manipulation"
        configurations_tested: 18
        model_sizes_tested: [32, 64, 128]
        data_volumes_tested: [500, 1000, 2000]
        loss_types_tested: ["baseline", "temporal_consistency"]
        key_metrics:
          small_model_avg_improvement: "+5.18%"
          medium_model_avg_improvement: "-3.84%"
          large_model_avg_improvement: "-5.85%"
          best_configuration: "h32_n2000_temporal_consistency"
          best_loss: 0.007614
          data_scaling_effect: "neutral"
          model_scaling_effect: "negative"
        key_insights:
          - "Temporal consistency helps small models (+5.18%) but hurts large models (-5.85%)"
          - "Over-regularization effect: larger models have capacity to learn directly, auxiliary losses constrain them"
          - "No data scaling effect: improvement doesn't increase with more data"
          - "Best config: small model (h=32) + more data (n=2000) + temporal consistency"
          - "Regularization-capacity tradeoff discovered"
        recommendations:
          - "R1: Use temporal consistency loss only for small models (hidden_dim <= 32)"
          - "R2: For larger models, rely on data volume rather than auxiliary regularization"
          - "R3: Investigate adaptive regularization that scales with model capacity"
          - "R4: Test whether this finding generalizes to real robot data"
    
    - experiment_id: H1.470.1.1.35
      description: "Test experience replay + auxiliary losses for multi-step tasks"
      result:
        conclusion: "INCONCLUSIVE"
        task: "multi_step_manipulation"
        configurations_tested: 6
        loss_types_tested: ["baseline", "temporal_consistency", "experience_replay", "replay_tc", "prioritized_replay_tc", "ewc_tc"]
        key_metrics:
          baseline_test_loss: 0.022540
          temporal_consistency_test_loss: 0.022448
          experience_replay_test_loss: 0.022538
          replay_tc_test_loss: 0.022619
          prioritized_replay_tc_test_loss: 0.022507
          ewc_tc_test_loss: 0.022629
          best_config: "Temporal Consistency"
          best_test_loss: 0.022448
          best_improvement_percent: 0.41
